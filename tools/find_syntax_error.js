const fs=require('fs');const s=fs.readFileSync('index.js','utf8');let lo=0,hi=s.length;let bad=hi;while(lo<hi){const mid=Math.floor((lo+hi)/2);try{ new Function(s.slice(0,mid)); lo=mid+1;}catch(e){ bad=mid; hi=mid;} } const before = s.slice(Math.max(0,bad-120), bad+40); const linesBefore = s.slice(0,bad).split('\n'); console.log('bad_index',bad,'line',linesBefore.length); console.log('context:\n---\n'+before+'\n---');
(function () {
  // Set this to your Cybrancee public URL when the static frontend runs on Render.
  // Keep it empty for same-origin local hosting.
  window.SCRIMS_API_BASE = window.SCRIMS_API_BASE || '';
})();
